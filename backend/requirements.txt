fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.28
python-dotenv>=1.0.1
httpx>=0.27.0
requests>=2.31.0
groq>=0.9.0
psycopg[binary]>=3.1.18
pytest>=8.0.0
python-multipart>=0.0.9
pillow>=10.2.0
huggingface_hub>=0.26.0
google-genai>=1.0.0
gTTS>=2.5.0
mutagen>=1.47.0

# Stable Diffusion 1.5 (local image provider) is NOT listed here on purpose --
# `diffusers` and `torch` are large, GPU-oriented packages not needed by the rest
# of the app, and the test suite never requires them. Install separately only if
# you want this provider active: see README section 8b, "Stable Diffusion 1.5
# (Local) Setup", for the exact pip command matching your machine (CPU vs CUDA).
